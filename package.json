{
  "dependencies": {
    "p5": "^2.2.1",
    "tinyqueue": "^3.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.3.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist"
  }
}
